<h1>Ajouter un avis pour <%= @restaurant.name %></h1>

<%= simple_form_for [@restaurant, @review] do |form| %>
  <%= form.input :rating %>
  <%= form.input :content %>

  <%= form.button :submit, 'Ajouter l\'avis' %>
<% end %>

<%= link_to 'Retourner à la liste', restaurant_path(@restaurant) %>
